#ifndef _GLOB_HPP_
#define _GLOB_HPP_

#include <iostream>
#include <cstdlib>
#include <string>

using namespace std;

void pause();
void limpaTela();
int stringToInt(const string numero);
bool isDigit(const char dig);
int charToDigit(const char ch);

#endif